# 2026-04-26 — 簡易銷貨單流程上線（任務 1 + 任務 2）

## 摘要

新建一條「Google Sheet 撿貨員一鍵建單」流程，跟現有 sales_orders 完全隔離。

撿貨員在 `撿貨表-YYYY-MM-DD` 分頁按一鍵建單 → 寫入新表 `simple_sales_orders` / `simple_sales_details` → 同步到「當日銷貨單成立表-YYYY-MM-DD」。

---

## 完成項目

### 任務 1：DB schema + RPC + secret + 安全測試

**Migration 檔案**（在 `docs/sql/migrations/`，已 .gitignore 不入 git）：
- `2026-04-26_simple_sales_orders.sql`：建表 + RLS + 9 支 RPC（v3.1）
- `2026-04-26_simple_sales_secrets.sql.example`：secret 設定範本
- `2026-04-26_simple_sales_smoke_tests.sql`：4 項安全測試
- `2026-04-26_simple_sales_orders_v3.2.sql`：補強 RPC（自動抓 products 價格 + 重複偵測 + 找不到/無價時 RAISE）

**新建 DB 物件**：
- 表：`simple_sales_orders`（17 欄含 wave_id unique index）、`simple_sales_details`（19 欄含 CHECK）、`simple_sales_config`（藏 secret）
- RPC：9 支（2 helper + 7 對外）
  - `_simple_check_picker_secret` / `_simple_check_store_secret`（內部 helper，已 REVOKE FROM PUBLIC）
  - `simple_create_sales_order`：v3.2 自動從 products 抓 price_branch / cost / unit、偵測重複建單回 `{duplicate, order_no}`、找不到商品/無價就 RAISE
  - `simple_submit_return_report` / `simple_confirm_received` / `simple_revoke_received`：店家用，需傳 store_secret 且 store_name 對得上
  - `simple_admin_process_return`：admin 用，限制 return_status IN (申請中,同意) 才能改
  - `simple_get_store_orders` / `simple_get_order_details`：店家查單，需傳 store_secret + store_name
- Secret：1 組 picker_secret + 18 組 store_secret（每店一組）

**安全測試（4 項全通過）**：
- TEST 1：picker_secret 建單成功
- TEST 2：錯誤 store_secret 被擋
- TEST 3：A 店 secret 查 B 店資料被擋
- TEST 4：店家退貨只能改自己店的明細（A secret + B 店名擋；B secret 改 A detail 擋；A secret + A 店名 + A detail 成功）

**事故與處理**：
- 一次 secret 洩漏：使用者把 19 組 secret 完整貼到對話。已用 `ON CONFLICT DO UPDATE` 重新產生覆蓋舊值，舊 secret 全部失效。

### 任務 2：Apps Script 一鍵建單

**改動的檔案**：
- `google-apps-script/16staff_lookup_code.gs`
  - `STORES` 加 `'龍潭'`（17 → 18 家）
  - `STORE_EMAILS` 加 `'龍潭': []`（之後填）
  - 新增 `PICKER_EMAILS` 常數（= ADMIN_EMAILS + 額外撿貨員）
  - `onOpen` 加三層權限分層：admin 看完整選單、picker 看「🛠️ 撿貨工具」（一鍵建單 + 樂樂匯入）、其他看「🛠️ 訂貨工具」
  - 新增 7 個函式：`createSalesOrdersFromPicking` / `_readPickingSheet_` / `_filterItemsForStore_` / `_callSimpleCreateOrder_` / `_writeResultSheet_` / `_ensureResultSheet_` / `_addOneDay_`

**沒動到的東西**：
- 樂樂匯入、結單鎖定、欄位保護、JSON 匯入、syncQuanminOnly、createMonthTab_、importProductsFromERP、addProducts、syncToSupabase 全部保留。

**驗收測試**：
- 一鍵建單成功 1 張（全民店、SS-20260426-001、$4,700、46 件）
- 第 2 次點同撿貨表 → duplicate 偵測通過（建單成功 0、已建過跳過 1）
- 16 個無 price_branch 商品的店被正確擋下，錯誤訊息明確指出商品編號

---

## 部署交接清單

### 安裝（新 Spreadsheet 啟用此流程時）

1. **Apps Script 程式碼**：把 `google-apps-script/16staff_lookup_code.gs` 全文貼到 Apps Script 編輯器
2. **Script Properties**：⚙️ 專案設定 → 指令碼屬性 → 新增 `PICKER_SECRET`（值 = 從 Supabase 取得的 picker_secret）
3. **第一次跑**：F5 重整 Sheet → 看到「🛠️ 開團管理」選單 → 跑任意一個現有功能（會跳授權）→ 接受授權

### 撿貨員每日流程

1. 從月份分頁複製某結單日的列 → 新建分頁，命名 `撿貨表-YYYY-MM-DD`
2. 確認 18 家店欄位都在（含龍潭，順序：三峽中和文山四號永和忠順環球平鎮古華林口南平泰山萬華湖口經國松山全民龍潭）
3. 撿貨完，數量歸零代表沒撿、>0 代表撿到的數量
4. 選單 → 🛠️ 開團管理 → 📦 撿貨完成 → 一鍵建單 → 確認
5. 結果寫入「當日銷貨單成立表-YYYY-MM-DD」

### 常見錯誤與排解

| 錯誤訊息 | 原因 | 排解 |
|---------|------|------|
| Script Properties 未設定 PICKER_SECRET | 沒設 secret | 到 Apps Script 設定 |
| 您不在撿貨員白名單 | 該帳號不在 PICKER_EMAILS | 在 16staff_lookup_code.gs `PICKER_EMAILS` 加 email |
| 商品「XXX」在 products 表找不到 | 編號錯或產品未建檔 | 到 ERP ProductList 建商品或修正編號 |
| 商品「XXX」的 price_branch 未設定或 ≤ 0 | 該商品分店單價沒填 | 到 ERP ProductList 設 price_branch |
| 此撿貨表「...」已為「店名」建過單 | 重複建單（duplicate 偵測） | 正常行為，請確認原單號是否正確 |

### secret 管理

- 19 組 secret 都存在 Supabase `simple_sales_config` 表
- **絕不貼進對話 / git / Slack / 公開頁面**
- 換 secret 流程：`UPDATE simple_sales_config SET config_value = ... WHERE config_key = '...';` 然後到對應 Apps Script Properties 同步更新
- secret 洩漏處置：立即用 `INSERT ... ON CONFLICT DO UPDATE` + `gen_random_uuid()` 全量重產

### 任務 2.5：到貨制撿貨表流程（arrival-based picking sheet）

**動機**：原任務 2 假設「依結單日批次建撿貨表」，但實際撿貨員是依**廠商實際到貨的商品**撿（同一張撿貨表上可能有 4/1、4/6、4/10 不同結單日的商品）。撿完後撿貨員會列印拿紙本去現場撿，回來才填實撿。

**改動的檔案**：
- `google-apps-script/16staff_lookup_code.gs`（1515 → 1924 行）：加 onOpen 兩個新選單、`createTodayPickingSheet`、`_formatPickingSheetHeader_`、`showPickingSearchSidebar`、`searchProductsAcrossMonths`、`addPickedProductsToPickingSheet`、`_writeDiffSheet_`、`_computeDiff_`、`_summarizeDiff_`、`_parseExpectedFromNote_`；改寫 `_readPickingSheet_` 改用 cell value（實撿）+ getNote（應撿）；改 `createSalesOrdersFromPicking` 在建單後產生差額表
- `google-apps-script/PickingSearchSidebar.html`（新建，335 行）：搜尋框 + 卡片清單 + checkbox 勾選 + 批次加入
- `normalizeDate_` 加 Date 物件支援（修月份分頁的「結單日」欄是 Date 物件被 String() 變長字串的 bug）

**關鍵設計**：
- 撿貨表 23 欄（A 狀態 / B 結單日 / C 編號 / D 名稱 / E 合計 / F~W 18 家店）— 列印友善
- 各店欄位 cell value = 實撿（撿貨員回來填）；setNote `應撿:N` = 應撿（加商品時帶入訂購量）
- 列印時看到的數字是應撿（撿貨員拿著去現場撿），回來覆寫成實撿
- 建單後自動比對應撿 vs 實撿 → 寫「今日撿貨差額表-YYYY-MM-DD」分頁（顯示哪間店多/少）
- Sidebar 跨月份分頁搜尋（編號 OR 名稱），支援多筆勾選批次加入

**RPC 沒改**：v3.2 RPC 完全沒動，建單仍用實撿數量（cell value）當 qty。差額表由 Apps Script 算，不寫進 DB。

**安全強化**：
- `setNotes` 用 `''` 不用 `null`（Apps Script 行為一致）
- 差額表重跑時 `clearContent()` 第 2 列起再寫（避免累加）
- 搜尋過濾 `totalQty <= 0` 的空商品（避免員工誤勾）
- B 結單日欄、C 商品編號欄寫入前 `setNumberFormat('@')`（避免自動轉日期格式或長編號變科學記號）

**驗收測試（2026-04-26）**：
- 建空白撿貨表 ✅
- Sidebar 跨月搜「雲林二」找到 7 筆，勾 4 筆批次加入 ✅
- 撿貨表卡片日期 4/19 顯示正確（修 Date bug 後） ✅
- 改實撿（覆寫應撿）→ 一鍵建單 → 4 張單成功 ✅
- 差額表自動產生：少給 2 筆（三峽 -2、中和 -4）、多給 3 筆（文山 +1、三峽 +4、三峽 +2）、其餘持平 ✅
- 重跑同撿貨表 → DB duplicate 偵測擋住、差額表覆蓋不累加 ✅

### 任務 3：店家 Sheet 第一版（隔日進貨單 / 所有進貨單 / 確認收貨）

**動機**：每家店一份獨立 Spreadsheet，副帳號 `w161616w@gmail.com` 持有，店家用副帳號操作。透過 store_secret 限制只能看到自己店資料，跟舊 portal 的 sales_orders 完全隔離。

**新增檔案**：
- `google-apps-script/store_sheet_code.gs`：店家 Sheet Apps Script，含 onOpen 選單、setupStoreSheets、refreshTomorrowDeliveries、refreshAllDeliveries、openOrderDetailsSidebar 與 dialog callbacks（getOrderDetailsForSidebar / confirmReceivedFromSidebar / revokeReceivedFromSidebar）
- `google-apps-script/StoreOrderSidebar.html`：modeless dialog 內容（單號標題卡 + 明細表 + 確認收貨 / 撤銷收貨按鈕）

**安裝（每家店一份）**：
1. 副帳號開新 Spreadsheet
2. 擴充功能 → Apps Script → 貼 `store_sheet_code.gs` 全文
3. 新增 HTML 檔 `StoreOrderSidebar` 貼入
4. 專案設定 → 指令碼屬性新增：
   - `STORE_NAME`（如「全民」，必須是 18 家店清單其一）
   - `STORE_SECRET`（從 `simple_sales_config.store_secret:{店名}` 取得）
5. 重整 Sheet → 「🛠️ 進貨工具 → ⚙️ 初始化分頁」建好 4 個分頁

**4 個分頁**：
- 隔日進貨單：filter `delivery_date == tomorrow`（client side filter 於 30 天 fetch 後）
- 所有進貨單：最近 30 天
- 店轉店（占位，等任務 6）
- 需求表（占位，等任務 7）

**選單 4 項**：
- 🔄 刷新隔日進貨單
- 📋 刷新所有進貨單
- 🔍 查單號明細（先看 active cell A 欄是否 `SS-` 開頭單號 → 直接開；否則 prompt 輸入）
- ⚙️ 初始化分頁（第一次安裝）

**關鍵設計**：
- Sidebar 寬度被 GAS 鎖死（~300px），改用 `showModelessDialog` 900x650
- modeless dialog 不擋 Sheet 操作、可拖移
- 跨店資料隔離：A 店 secret + B 店 store_name 一律失敗（DB 端 `_simple_check_store_secret` 擋）
- 主表 30 天內的單從 fetch 結果找；超過 30 天則用 fallback object 並顯示「超過範圍」警示，明細仍可正常顯示

**驗收測試（2026-04-26 晚）**：
- 全民店初始化分頁成功 ✅
- 切到松山店（改 STORE_NAME + STORE_SECRET）→ 「📋 刷新所有進貨單」拉到 SS-20260426-015（13 件、$1,530、已建單）✅
- 點 A 欄單號 → 「🔍 查單號明細」→ dialog 顯示 3 筆明細 ✅
- 「✅ 確認收貨」→ 狀態變「已收到」→ dialog reload 顯示「↩️ 撤銷收貨」✅
- 「↩️ 撤銷收貨」→ 狀態回「已建單」✅

**事故與處理**：
- 使用者兩次截圖露出全民 secret（指令碼屬性畫面 + debug log），每次用 `UPDATE simple_sales_config SET config_value = replace(gen_random_uuid()::text,'-','') || replace(gen_random_uuid()::text,'-','')` 重產

**未實作（留給任務 5）**：
- dialog 沒有「📤 退貨」按鈕
- 店家無法在 Sheet 申請退貨（必須等任務 5 加按鈕 + admin 處理介面）
- 退貨**顯示**已完成（主表退貨狀態欄、dialog 退貨徽章 + 明細橘底 + admin 回應），**操作**還沒做

### 任務 5：退貨流程（店家申請 + admin 處理）— 跳過任務 4 提前做

**動機**：simple_sales_orders 跟舊 portal sales_orders 完全切開，退貨流程必須在新系統內閉環（店家 Sheet 申請 → admin 在 16staff Sheet 處理），不連 lt-erp。原計畫順序是 4 → 5，因為退貨是店家上線的 blocker，跳過 4 提前做 5。

**A 段 — DB**（migration 受 `docs/sql/*` .gitignore 排除，不入 git）：
- `2026-04-26_simple_admin_secret.sql`（主 migration）
- `2026-04-26_simple_admin_secret_init.sql.example`（admin_secret 初始化範本）
- DROP 舊 `simple_admin_process_return(BIGINT,TEXT,TEXT)`（grep 確認無呼叫端）
- 新版 `simple_admin_process_return(JSONB)`：admin_secret 或 JWT role **二擇一驗證**（讓未來簡易網站登入 admin 也能用，不被迫綁 secret）
- 新 `simple_get_pending_returns(TEXT, BOOLEAN)`：admin 退貨待辦 list；SQL 端限制已結案範圍 30 天，避免 Apps Script 拉太多再篩
- 新 helper `_simple_check_admin_secret`（REVOKE FROM PUBLIC）

**B 段 — 店家 Sheet**（修 `store_sheet_code.gs` + `StoreOrderSidebar.html`）：
- 加 `submitReturnFromSidebar` 函式（後端再驗 + 呼叫 `simple_submit_return_report` RPC）
- 明細表加「動作」欄
- 退貨按鈕**顯式白名單**：只在 `status='已建單' OR '有問題'` + `return_status='無'` + `qty>0` 時顯示（避免主表 30 天外 status='?' 也開放退貨）
- 點按鈕展開行內表單（類型 損/少/缺/退、數量、原因），三層驗證（前端 + .gs + RPC）
- 數量限制：整數、>= 1、<= qty
- 順手修：`pendingSuccessMsg` 機制讓 confirm/revoke/退貨 的 success 訊息不被 load() 清掉

**C 段 — 16staff Sheet**（修 `16staff_lookup_code.gs` + 新檔 `ReturnProcessDialog.html`）：
- admin 選單加 2 項：「🔁 刷新退貨待辦」/「🔧 處理選定退貨」
- 新分頁「🔁 退貨待辦」，13 欄（含隱藏 detail_id）
- 狀態著色：申請中=紅、同意=橘、拒絕=灰、已收到=綠、免退=紫
- 排序：未結案優先，其內按申請時間舊→新（最早申請先處理）
- modeless dialog 900x650：標題卡 + 退貨資訊卡 + admin 回應 textarea + 動作說明 + 4 顆按鈕
- 4 種處理動作：✅ 同意（中間狀態，待寄回）/ 📦 已收到（終態，結案）/ ❌ 拒絕（終態）/ ⛔ 免退（終態）
- 二次 confirm 明確說明同意是中間態 vs 其他終態
- `ADMIN_SECRET` 從 Script Properties 取（同 PICKER_SECRET 模式）

**設計重點**：
- 模型 B（任務 1 已定）：申請中 + 同意 都算未結案，admin 必須最終處理為終態（已收到 / 拒絕 / 免退）才算「已解決」
- 三層驗證：DB store_secret/admin_secret + Apps Script 後端再驗 + 前端 UI 驗證
- 第一版**店家退錯不能撤回**（DB 沒對應 RPC），由 admin 用「拒絕」結案

**驗收測試（2026-04-26 晚）**：
- 全民店 detail 520（一口鍋鍋粑脆餅-海苔椒鹽 6 件）退貨完整 lifecycle 跑通：
  - 店家 dialog 申請退貨：類型損 / 數量 6 / 原因「外包裝壓壞」→ DB return_status='申請中' ✅
  - admin「🔁 刷新退貨待辦」→ 紅底 1 筆 ✅
  - admin「🔧 處理選定退貨」→ dialog → 填回應 → 「✅ 同意」→ 橘底「同意」✅
  - 再「🔧 處理選定退貨」→「📦 已收到」→ 綠底「已收到」✅
  - DB 驗證：detail 520 return_status='已收到'

**事故與處理**：
- ADMIN_SECRET 第一次貼成 165 字元（多複製了內容）→ debugAdminSecret 函式對比 DB 長度發現，重 rotate + 用 `'|' || ... || '|'` 標邊界後重貼成 64 字元解決
- 設定指令碼屬性混亂：使用者把 ADMIN_SECRET 加到松山店 Sheet（錯位置），釐清後改加到 16staff Sheet（撿貨員 / admin 用），松山店多餘的 ADMIN_SECRET 待清理

---

### 未來任務（接下來要做的）

- **任務 3**：17 個店家 Sheet 模板 + IMPORTRANGE 預先授權
- **任務 4**：簡易銷貨單網站（總倉撿貨員列表 + 列印）
- **任務 5**：店家 Sheet 退貨回報雙向同步
- **任務 6**：店家 Sheet 店轉店雙向同步（含 transfers DB 連動）
- **任務 7**：店家 Sheet 需求表雙向同步
- **任務 8**：結單一鍵開關（portal 結單填表自動唯讀）
- **任務 9**：lt-erp MonthlyReport / Receivablereport 改抓新表
- **任務 10**：每 10 天封存（封存後設唯讀）

---

## 重點注意

1. `simple_*` 系統跟現有 `sales_orders` / `branchOrders` 完全隔離，不會相互影響
2. `simple_sales_orders.wave_id` + `store_name` 有 unique index，DB 層擋重複建單；Apps Script 收到 `{duplicate:true, order_no}` 後寫到成立表的「已建過」欄
3. 18 家店 STORES 順序對齊撿貨表欄位（F~W），龍潭是第 18 家（W 欄）
4. RPC 全部 SECURITY DEFINER + secret 驗證（picker_secret 給撿貨員、store_secret:{店名} 給每家店）
5. 退貨流程模型 B：admin「同意」≠ 結案，須等變終態（已收到/拒絕/免退）才算「已解決」
